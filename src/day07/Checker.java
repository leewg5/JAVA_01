package day07;

public class Checker {
    boolean isEven( int x ){
        if( x % 2 == 0 ){
            return true;
        }else {
            return false;
        }
    } // func end
    // (int x ) : 정수 하나를 매개변수로 받아
    // boolean : 짝수이면 true, 홀수이면 false를 반환
} // class end